[package]
name = "kosmos-gpui"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
gpui = { version = "*" }
icondata_core = "0.1"
icondata_vs = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
