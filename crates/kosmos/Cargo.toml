[package]
name = "kosmos"
version = "0.1.0"
edition = "2024"

[dependencies]
gpui = { workspace = true }
icons = { path = "../icons" }
workspace = { path = "../workspace" }
tabs = { path = "../tabs" }
persistence = { path = "../persistence" }
