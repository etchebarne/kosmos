[package]
name = "installer"
version = "0.4.8"
edition = "2024"

[dependencies]
flate2 = "1"
registry = { path = "../registry" }
ureq = "2"
zip = { version = "2", default-features = false, features = ["deflate"] }
