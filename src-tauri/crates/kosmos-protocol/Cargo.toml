[package]
name = "kosmos-protocol"
version = "0.2.0"
edition = "2021"

[features]
async = ["dep:tokio"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["io-util"], optional = true }
